FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY scripts ./scripts
COPY content ./content
COPY agent-prompts ./agent-prompts

ENV NODE_ENV=production
ENV AGENT_MOCK=true
ENV AGENT_INTERVAL_MS=14400000

CMD ["node", "scripts/agent-scheduler.mjs"]